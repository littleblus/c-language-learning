#define _CRT_SECURE_NO_WARNINGS 1
#define maxn 105

#include <stdio.h>
#include <string.h>

int less(const char* s, int p, int q)
{
	int n = strlen(s);
	printf("%d\n", n);

	return 0;
}

int main()
{
	char s[maxn] = "abcdef";
	less(s, 0, 0);

	return 0;
}